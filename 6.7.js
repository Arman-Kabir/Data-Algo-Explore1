const string = "armankab";

function reverse(str) {
    console.log(str);
    let ar = [];
    let rev = [];
    for (let i = 0; i < str.length; i++) {
        ar[i] = str[i];
    }
    console.log(ar);
    for (let i = str.length-1; i >= 0; i--) {
        rev[i]=
    }
}

reverse(string);