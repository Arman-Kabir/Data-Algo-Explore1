// md5 hash Generator -- SHA-
// 256 hash generator
// Hash Function is used all across computer science - a hash function is simply a function
//  that generates a value of fixed length for each input that it gets

// We give it an input to hash function - and the function generates some random pattern.

// we have a key that is - grapes. We send it through a hash function that is going to
//  hash something really fast and map 